#secure-ssh.sh
#author inf
#creates a new ssh usere using $1 parameter
#adds a public key from the local repo or curled from the remote repo
#removes rppts ability to ssh in
echo "ALL YOUR CODE GOES HERE"

#now if I was actually making this for real then there would be a script but I dont have a repo for keys setup
